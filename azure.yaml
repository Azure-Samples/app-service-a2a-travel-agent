# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: semantic-kernel-travel-agent
metadata:
  template: semantic-kernel-travel-agent@0.0.1-beta
services:
  web:
    project: .
    language: py
    host: appservice
infra:
  provider: bicep
